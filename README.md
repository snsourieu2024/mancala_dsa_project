
# Mancala (DSA Gold)
CLI + optional GUI. Undo/redo. Minimax + alpha-beta + move ordering + TT. Tests.

## Run
python -m mancala.main
python -m mancala.main --depth 8
python -m mancala.main --gui   # optional if game2dboard installed

## Test
pytest -q
